generated: '2026-07-21'
method: searched
status: published
source: https://mcp.shakepay.com
notes: >-
  Live, publicly reachable Model Context Protocol server discovered via the
  Shakepay llms.txt "Agent experiments" section and the agent discovery page at
  https://shakepay.com/agents. This is an agent-access EXPERIMENT, not a general
  account-access API: Shakepay states "Public account-access tools are not
  currently available." The only tool exposed today is signal_interest, which
  records an agent's intended integration. Verified by a live JSON-RPC
  initialize + tools/list against the endpoint (no account access invoked; the
  signal_interest tool was NOT called).
server:
  name: shakepay-agent-access
  version: 1.0.0
  transport: http
  url: https://mcp.shakepay.com
  protocol_version: 2024-11-05
  discovery:
  - https://shakepay.com/agents
  - https://shakepay.com/llms.txt
tools:
- name: signal_interest
  description: >-
    Record agent interest in Shakepay integration paths. Public account-access
    tools are not currently available.
  required_inputs:
  - intended_use_case
  - expected_protocol   # enum: mcp | api | cli | browser | other
  - agent_runtime
  - human_initiated     # boolean
  - nonce               # current nonce from https://shakepay.com/agents
  - safest_first_integration
  optional_inputs:
  - additional_context
